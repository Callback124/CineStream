name: Build CineStream APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build-apk:
    name: Generate APK v2.0
    runs-on: ubuntu-latest

    steps:
      - name: 1. Checkout Repository
        uses: actions/checkout@v4

      - name: 2. Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 3. Setup Java 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: 4. Setup Android SDK 35
        uses: android-actions/setup-android@v3
        with:
          packages: 'platforms;android-35 build-tools;35.0.0'

      - name: 5. Install NPM Packages
        run: |
          npm install --no-audit --no-fund --legacy-peer-deps
          npm install --save @capacitor/core @capacitor/android @capacitor/cli --legacy-peer-deps

      - name: 6. Build Web Dist
        run: npm run build

      - name: 7. Init & Sync Capacitor Android
        run: |
          rm -rf android
          npx @capacitor/cli add android
          npx @capacitor/cli sync android

      - name: 8. Fix Kotlin Duplicate Classes Conflict
        run: |
          echo "Injecting Kotlin conflict fix in Gradle..."
          cat << 'EOF' >> android/app/build.gradle

          configurations.all {
              exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk7'
              exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk8'
          }
          EOF

      - name: 9. Build Debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug -x lint -x test -x check --no-daemon

      - name: 10. Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: CineStream-v2-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
